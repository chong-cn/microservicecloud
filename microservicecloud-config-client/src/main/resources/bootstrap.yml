spring:
  cloud:
    config:
      name: microservicecloud-config-client      # 从GitHub上读取的志愿名称，注意没有yml后缀名
      profile: dev                               # 本次访问的配置项
      label: master
      uri: http://config.com:3344                # 本微服务启动后先去找config服务，通过SpringCloudConfig获取GitHub的服务地址